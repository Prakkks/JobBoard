export enum STATUS {
  APPROVED = 1,
  PENDING = 2,
  REJECTED = 3
}

export enum JOBTYPE {
 FullTime = 1,
 PartTime = 2,
 Contract = 3,
 Internship = 4,
  _ = 0,
}

export enum USER_ROLE{
    admin=1,
    user=2,
    reviewer=3
}